# Nexus 测试目录结构

## 📁 目录结构

```
tests/
├── common/              # 通用测试工具
│   ├── helpers.rs      # 测试辅助函数
│   ├── fixtures.rs     # 测试数据和夹具
│   ├── utils.rs        # 测试工具函数
│   └── mocks.rs        # Mock 对象定义
│
├── unit/                # 单元测试（按架构分层）
│   ├── domain/         # Domain Layer 测试
│   │   ├── adapters/   # 适配器领域测试
│   │   └── config/     # 配置管理领域测试
│   ├── infrastructure/ # Infrastructure Layer 测试
│   │   └── queue/      # 队列基础设施测试
│   └── application/    # Application Layer 测试
│
├── integration/         # 集成测试
│   ├── api/           # API 集成测试
│   ├── end_to_end/    # 端到端测试
│   └── services/      # 服务集成测试
│
└── performance/        # 性能测试
```

## 🎯 设计原则

1. **按架构分层**：测试结构与源码架构对齐（Domain、Infrastructure、Application）
2. **模块化组织**：每个模块有独立的测试文件
3. **共享工具**：通用测试工具放在 `common/` 目录
4. **清晰命名**：测试文件命名清晰描述其测试内容

## 📝 使用说明

### 导入共享模块

每个需要使用 `common` 模块的测试文件需要添加：

```rust
mod common;
use common::*;
```

详细测试使用文档请参阅：[`tests/README.md`](../tests/README.md)
